/**
 * The single page. Sections get composed here in order, one import per section
 * from `@/components/sections`. Deliberately empty until sections are designed.
 */
export default function HomePage() {
  return <></>;
}
